#nested function calls - they are function calls inside other function calls
#                        innermost function calls are solved first
#                        returned value is used as an argument for the next outer function

print(round(abs(float(input("Enter a number: ")))))
